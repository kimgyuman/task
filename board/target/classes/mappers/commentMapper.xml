<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.mappers.comment">

	<!-- 댓글 조회 -->
	<select id="commentList" parameterType="int" resultType="com.board.domain.CommentVO">
		select
			C_seq_counsel, C_board_counsel, C_writer_counsel, C_content_counsel, C_regDate_counsel
		from C_counsel_info
			where C_board_counsel = #{C_board_counsel}		
	</select>

<!-- 댓글 작성 -->
<insert id="commentWrite" parameterType="com.board.domain.CommentVO" useGeneratedKeys="true" keyProperty="C_seq_counsel">
	insert into C_counsel_info(C_board_counsel, C_writer_counsel, C_content_counsel)
		value(#{C_board_counsel}, #{C_writer_counsel}, #{C_content_counsel})
</insert>
	
<!-- 댓글 수정 -->
<update id="commentUpdate" parameterType="com.board.domain.CommentVO">
	update C_counsel_info set
		<!--  C_writer_counsel = #{C_writer_counsel},-->
		C_content_counsel = #{C_content_counsel}
	where C_seq_counsel = #{C_seq_counsel}
		and C_board_counsel = #{C_board_counsel}	
</update>

<!-- 댓글 삭제 -->
<delete id="commentDelete" parameterType="com.board.domain.CommentVO">
	delete from C_counsel_info
	where C_seq_counsel = #{C_seq_counsel}
		and C_board_counsel = ${C_board_counsel}	
</delete>
	


<!-- 특정 댓글 조회 -->
<select id="commentSelect" resultType="com.board.domain.CommentVO">
 select
     C_seq_counsel, C_board_counsel, C_content_counsel, C_writer_counsel, C_regDate_counsel
 from C_counsel_info
     where C_seq_counsel = #{C_seq_counsel}
</select>


<!-- 특정 댓글 모두 삭제 -->
<delete id="commentDeleteAll" parameterType="com.board.domain.CommentVO">
	delete 
	from	C_counsel_info
	where	C_board_counsel = ${C_board_counsel}	
</delete>






</mapper>